import React from 'react';

// This component is temporarily disabled to resolve Google Cloud configuration issues.
// All authentication and sync logic has been removed from the app.
// Data is now saved locally in the browser.

const GoogleAuth: React.FC<any> = () => {
    return null; 
};

export default GoogleAuth;